<Window x:Class="WpfDemo.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        AutomationProperties.AutomationId="SettingsWindow"
        Title="Settings"
        Height="260"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Greeting settings"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16" />

        <StackPanel Grid.Row="1">
            <TextBlock Text="Greeting prefix" Margin="0,0,0,4" />
            <TextBox x:Name="GreetingPrefixTextBox"
                     AutomationProperties.AutomationId="GreetingPrefixTextBox"
                     AutomationProperties.Name="Greeting prefix"
                     Height="32"
                     Padding="8,4" />
        </StackPanel>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button AutomationProperties.AutomationId="CancelSettingsButton"
                    AutomationProperties.Name="Cancel"
                    Content="Cancel"
                    Width="90"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="CancelButton_Click" />
            <Button AutomationProperties.AutomationId="SaveSettingsButton"
                    AutomationProperties.Name="Save"
                    Content="Save"
                    Width="90"
                    Height="32"
                    Click="SaveButton_Click" />
        </StackPanel>
    </Grid>
</Window>
